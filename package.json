{
    "name": "healing-journeys",
    "version": "1.0.0",
    "description": "",
    "scripts": {
        "start": "rm -rf dist && rm -rf .parcel-cache && parcel ./src/pages/*.html --no-cache --no-source-maps",
        "build": "rm -rf build && rm -rf .parcel-cache && parcel build ./src/pages/*.html --no-cache --no-source-maps --dist-dir build",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "repository": {
        "type": "git",
        "url": "git+https://github.com/tranleminhhoang2410/Healing-Journeys.git"
    },
    "author": "",
    "license": "ISC",
    "bugs": {
        "url": "https://github.com/tranleminhhoang2410/Healing-Journeys/issues"
    },
    "homepage": "https://github.com/tranleminhhoang2410/Healing-Journeys#readme",
    "devDependencies": {
        "parcel": "^2.9.2",
        "posthtml-doctype": "^1.1.1",
        "posthtml-include": "^1.7.4"
    },
    "dependencies": {
        "@parcel/ts-utils": "^2.9.2"
    }
}
